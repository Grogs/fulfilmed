package me.gregd.cineworld.frontend

import me.gregd.cineworld.domain.{Movie, Performance}

import scala.concurrent.Future

class FilmController {
//  def loadMovies(cinemaId: String) = (state: FilmsState) => {
//    val films = Future.successful(Map[Movie, List[Performance]]())
//    films.map( res => state.copy(films = res))
//  }

}
